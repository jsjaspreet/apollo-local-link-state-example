import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header>
        <h1 className="App-title">Apollo Local Link State</h1>
      </header>
    );
  }
}

export default Header;